import { Container } from "semantic-ui-react"

export const CheckoutPage = () => {
  return (
    <Container text textAlign="center">
      This page would normally be a stripe-integrated checkout page and would therefore include stripe's ui.
    </Container>
  )
}
